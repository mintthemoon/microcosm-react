{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./",
    "rootDir": "./lib",
    "outDir": "./dist",
    "types": ["./types"],
    "paths": {
      "$lib": ["./lib"],
      "$lib/*": ["./lib/*"],
      "$util": ["./lib/utility"],
      "$util/*": ["./lib/utility/*"],
      "$types": ["./types"],
      "$types/*": ["./types"]
    }
  },
  "include": ["lib/**/*.ts", "lib/**/*.tsx"]
}
