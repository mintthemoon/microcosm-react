{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./",
    "declarationDir": "./dist/types",
    "paths": {
      "$lib": ["./lib"],
      "$lib/*": ["./lib/*"],
      "$util": ["./lib/utility"],
      "$util/*": ["./lib/utility/*"],
      "$types": ["./lib/types"],
      "$types/*": ["./lib/types"]
    }
  },
  "include": ["lib/**/*.ts", "lib/**/*.tsx", "__test__/*.ts"]
}
